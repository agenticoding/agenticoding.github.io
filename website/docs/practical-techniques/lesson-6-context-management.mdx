---
sidebar_position: 1
sidebar_label: 'Context Engineering'
sidebar_custom_props:
  sectionNumber: 6
title: 'Context Engineering'
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ContextPressureDiagram from '@site/src/components/VisualElements/ContextPressureDiagram';
import ContextStreamBlock from '@site/src/components/VisualElements/ContextStreamBlock';
import { contextManagementStream } from '@site/src/components/VisualElements/contextStreamData';
import PromptExample from '@site/src/components/PromptExample';
import MCPLoadingDecisionTree from '@site/src/components/VisualElements/MCPLoadingDecisionTree';
import ContextMechanismMap from '@site/src/components/VisualElements/ContextMechanismMap';
import ContextFilesHierarchy from '@site/src/components/VisualElements/ContextFilesHierarchy';
import SkillsInvocationDiagram from '@site/src/components/VisualElements/SkillsInvocationDiagram';
import SubAgentDiagram from '@site/src/components/VisualElements/SubAgentDiagram';
import ContextSqueezeDiagram from '@site/src/components/VisualElements/ContextSqueezeDiagram';
import ScrollDrivenFigure from '@site/src/components/animations/ScrollDrivenFigure';
import MCPToolSchemaDiagram from '@site/src/components/VisualElements/MCPToolSchemaDiagram';
import ArchitectureDiagram from '@site/src/components/VisualElements/ArchitectureDiagram';
import UShapeAttentionCurve from '@site/src/components/VisualElements/UShapeAttentionCurve';

Agents do not use a context window uniformly. They attend to some tokens more strongly than others, and position matters as much as raw capacity. Context engineering is the practice of shaping what enters the window, where it lands, and when it appears so the task stays in the model's high-attention zone.

[Lesson 5: Grounding](/methodology/lesson-5-grounding) covered how to retrieve the code, docs, and external sources that make the agent operate against reality. Context engineering starts after that. Once the right information exists, you still have to decide how much enters the window, where it lands, and when it expires.

The core problem is positional. The [lost-in-the-middle effect](/fundamentals/lesson-1-how-llms-work) means the same instruction can be reliable at the top or bottom of the window and unreliable in the middle. A 200K-token window sounds enormous until you realize a single MCP server can consume 55K tokens just for tool schemas — before the agent reads a single line of your code. Capacity is not the goal. Controlled attention is the goal.

This lesson covers the five mechanisms that decide what enters the context window and where: **context files**, **MCP tool schemas**, **skills**, **sub-agents**, and **runtime management**. Each operates at a different layer, with different tradeoffs between token cost and attention reliability.

## The Context Pressure Problem

As you add context files, connect MCP servers, and accumulate conversation history, the fixed-size "User Task" band — your actual request — drifts deeper into the middle of the window. The attention curve shows what happens: the task enters the forgotten zone where the agent can no longer reliably follow your instructions.

<ContextPressureDiagram />

Try the presets. **Fresh Session** keeps the window light — the task gets strong attention. **Heavy MCP (eager)** shows the real danger: connecting a few MCP servers plus conversation pushes the task into the dead zone, even though the window isn't full. **Heavy MCP (deferred)** demonstrates the dramatic savings from deferred tool loading — same tool count, fraction of the tokens. **Deep Conversation** shows how long conversations alone create pressure — 15 turns of accumulated context push the task down. **Skill-Heavy** shows procedure expansions: turns where a skill or reusable workflow was invoked get a violet accent bar, and their extra 4K tokens per expansion add up fast. **Near Compaction** shows the drain cascade in action — the hatched region models capacity reserved for the compaction handoff, shrinking the effective working window so the agent can still summarize before it hits the wall. **Overloaded** is the worst case — everything maxed out, task completely invisible.

The connector on the right side shows how tool definitions produce results that land in conversation turns. Notice how the drain cascade works: when total content exceeds the effective window, headroom disappears first, then conversation gets compacted (reduced opacity bands), then context files shrink, and finally tool definitions yield — each slider shows the requested-vs-actual gap when drained.

The key insight: **it's not about running out of tokens — it's about where your task lands relative to the attention curve.** A 65% full window with heavy tool definitions is worse than an 80% full window where most content is conversation (which sits below the task in the recency zone).


## Capacity Is Not Reliability

Large context windows have legitimate uses: summarizing regulatory filings, reviewing legal contracts, processing long transcripts. Those are retrieval-heavy tasks where finding stated facts scales reasonably well with window size. Agentic coding is different. Coding agents connect constraints across files, follow multi-step logic, and reason about architectural relationships. That is inference, and inference degrades as context grows.

The mistake is treating **capacity** as **reliability**:

- **Advertised context** is how many tokens the model accepts.
- **Effective context** is how much of that input the model can still use reliably.
- **Point drop** is absolute percentage-point loss. A fall from 80% to 40% is −40 percentage points, even though relative accuracy was cut in half.
- **Attention placement** determines whether the important facts are visible when the model needs them.

<UShapeAttentionCurve />

**Attention is asymmetric under load.** At moderate fill, attention forms a U-shape: strong at both ends, weak in the middle. As context fills past 50%, recency dominates, primacy weakens, and the middle becomes effectively invisible. This is transformer architecture under realistic constraints, not a bug.

Current long-context benchmarks show the pattern. Simple retrieval can hold up across large windows. Inferential tasks are more fragile: Anthropic's [MRCR v2 8-needle benchmark](https://claude.com/blog/1m-context-ga) shows large point drops near maximum context, while [NoLiMa](https://arxiv.org/abs/2502.05167) shows a 29-point drop within 32K tokens on tasks requiring latent reasoning.

The context window is storage. Attention is bandwidth. Context engineering exists because agentic coding fails when you treat storage as bandwidth.

<details>
<summary>Long-context benchmark evidence</summary>

MRCR means **Multi-Round Co-reference Retrieval**. The model gets a very long prompt with multiple target facts buried inside distractor text, then must answer questions that refer to those facts indirectly. The **8-needle** variant plants eight facts, so the model has to keep several references straight instead of matching one obvious phrase.

MRCR stresses the exact failure mode agentic coding creates: after search results, file reads, logs, and partial plans flood the context, can the agent still recover the few facts that matter?

:::info Why this is different from coding benchmarks
MRCR does **not** measure patch quality. Coding benchmarks like SWE-bench test debugging, repository reasoning, tool use, and patch generation. MRCR tests long-context retrieval under pressure. A model can improve on coding benchmarks while regressing on MRCR because these are related but different capabilities.
:::

At moderate context, frontier models cluster tightly. Near the advertised maximum, they diverge brutally.

| Model | Window | Moderate context | Long context | Point drop | Pattern |
|---|---:|---:|---:|---:|---|
| Claude Sonnet 4.6 | 1M | 90.6% @256K | 65.1% @1M | −25.5 pp | Gradual decay |
| Claude Opus 4.6 | 1M | **93.0%** @128K | **76.0%** @1M | −17.0 pp | Strong retention |
| Claude Opus 4.7 | 1M | 78.3%⁠* | 32.2% @1M | −46.1 pp | Release regression |
| Claude Opus 4.8 | 1M | 85.9%⁠† | 68.1%⁠† | −17.8 pp | Partial recovery |
| GPT-5.4 | 1.05M | 79.3% @128K | 36.6% @1M | −42.7 pp | Sharp collapse |
| GPT-5.5 | 1.1M | 87.5%⁠* | **74.0%** @1M | −13.5 pp | Retrieval retrain |
| Gemini 3.1 Pro | 1M | 84.9% @128K | 26.3% @1M | −58.6 pp | Severe collapse |
| Gemini 3.5 Flash | 1M | 77.3% @128K | 26.6% @1M | −50.7 pp | Severe Flash-tier decay |
| DeepSeek V4 Pro | 1M | 83.5%⁠^ | ~66% @1M | ~−17 pp | Compression tax |
| DeepSeek V4 Flash | 1M | 89% @128K | 58–79%⁠^ @1M | 10–31 pp | Reasoning-mode dependent |

<details>
<summary>How to compare these numbers</summary>

Use the table for the **shape of degradation**, not as a strict leaderboard. Most rows are MRCR v2 8-needle long-context retrieval; marked rows need context:

- `*` uses 256K as the moderate-context point because the relevant 128K value was not the published comparison point.
- `^` shows Max reasoning mode. DeepSeek defaults are lower: Pro ~66%, Flash 58% @1M.
- `†` uses GraphWalks BFS, a harder relational-retrieval benchmark, because Opus 4.8's MRCR v2 score is unpublished. Opus 4.8 also scores 83.3% on GraphWalks Parents @1M.
- Gemini 3.5 Flash publishes 128K average and 1M pointwise scores, but no 512K breakdown.
</details>

The table tells three stories: same window ≠ same reliability; moderate context is not the problem; the degradation curve matters more than the headline window.

:::tip First-pass takeaway
Every model has an effective context limit. Some decline gradually, some fall off a cliff, and some recover only with expensive reasoning modes. None make context management obsolete.
:::

<details>
<summary>Claude: coding wins can damage retrieval</summary>

Opus 4.6 was purpose-built for long-context retrieval: Extended Thinking gave it a dedicated reasoning budget to "search" the 1M-token space. Opus 4.7 removed that path, gained major wins in coding, visual reasoning, and tool orchestration, but fell from 78.3% to 32.2% on long-context retrieval — a 59% relative collapse.

Opus 4.8 partially corrects the regression: its GraphWalks BFS @1M beats Opus 4.6 on the same subtask, but MRCR v2 remains unpublished. The lesson: **the same changes that improve coding can damage long-context retrieval.**
</details>

<details>
<summary>GPT: from bigger window to retrieval retraining</summary>

GPT-5.4 moved back into the 1M-token tier, but exposed a reliability cliff: 79.3% around 128K, 57.5% around 256K–512K, and 36.6% near 1M. It could accept the tokens, but could not use them reliably.

GPT-5.5 appears to be the correction: retrained for long-context retrieval with heavier long-document examples, better position-aware behavior, and lower distractor susceptibility. Its 74.0% near 1M is roughly double GPT-5.4. The lesson is not "GPT wins"; it is that reliability requires model-level optimization, not just a bigger window.
</details>

<details>
<summary>Gemini: capacity without reliable use</summary>

Gemini 3.1 Pro advertises the same 1M-token class as Opus and GPT, yet drops to 26.3% near 1M. Large windows still help retrieval-heavy workflows, but they do not make agentic coding safe by default.
</details>

<details>
<summary>DeepSeek: cheap 1M context has a precision trade-off</summary>

DeepSeek buys efficiency with hybrid attention: CSA compression plus sparse selection, and HCA dense scanning. That makes 1M-token inference cheaper, but consistency depends on reasoning mode. V4 Flash collapses in non-thinking mode; Max mode recovers to 78.7%. Compression buys efficiency, but reasoning-time compute has to recover precision.
</details>

</details>

:::warning The Agentic Coding Cost Trap

Most providers apply per-token surcharges past a context threshold:

- **Gemini 3.1 Pro:** 2× input pricing above 200K tokens
- **GPT-5.5 / GPT-5.4:** 2× input pricing above ~272K tokens
- **DeepSeek V4 Pro / Flash:** 1.5× pricing above 512K tokens
- **Claude 4.6/4.7/4.8 (Opus/Sonnet):** No surcharge — flat rate to 1M

For retrieval tasks (summarization, document review), the surcharge buys tokens in the model's strong accuracy zone. For agentic coding, you're paying a premium for tokens in the model's *worst* accuracy zone — the middle, where your constraints and requirements go to die.
:::

## Retrieval Is Not Context Engineering

Semantic search, code research, and web research improve discovery. They help the agent find the right facts by meaning instead of guessing from keywords or training priors. But retrieval alone does not decide placement, lifetime, or isolation.

Three Grep searches can return 18,000 tokens. Reading five files can add another 22,000. You are at 40,000 tokens before discovery is finished. Ten excellent semantic chunks can cost 15,000 tokens; reading related files adds 25,000; exploring patterns adds another 10,000. You are at 50,000 tokens before implementation starts.

That is why correct retrieval can still hurt reliability. A long search trail can push the actual task into the forgotten middle. A useful web page can arrive too early, sit too long, and compete with more relevant instructions later. Coherent distractor text is especially dangerous because it looks relevant while degrading reasoning more than obvious noise ([Chroma Context Rot study](https://research.trychroma.com/context-rot), 2025).

So treat retrieval as the input to context engineering, not a replacement for it. Grounding answers **what facts should enter the agent's world**. Context engineering answers **how those facts stay usable**.

## The Five Context Mechanisms

<figure>
  <ContextMechanismMap />
  <figcaption>
    The five context mechanisms: where tokens land and when they load.
  </figcaption>
</figure>

The spectrum runs from **always-loaded** (context files — guaranteed delivery, high cost) to **fully isolated** (sub-agents — zero cost to parent, but no shared state). Effective context engineering means choosing the right mechanism for each piece of knowledge.

## Context Files (AGENTS.md) {#context-files-claudemd--agentsmd}

Context files are markdown documents injected between the system prompt and your input — the primacy zone. They give agents project memory without repeated explanations: architecture, conventions, build commands, constraints.

The tradeoff: **guaranteed delivery vs. pushing everything else toward the middle.** Every token in your context files shifts the user task band down.

<ScrollDrivenFigure
  phaseEnd={0.5}
  caption="The Primacy Squeeze: as AGENTS.md grows, the user task gets pushed from the primacy zone into the forgotten middle."
>
  <ContextSqueezeDiagram />
</ScrollDrivenFigure>

`AGENTS.md` is the vendor-neutral standard adopted by 60,000+ open-source projects (now governed by the Linux Foundation's Agentic AI Foundation), working across GitHub Copilot, Cursor, Windsurf, and most other AI coding tools (note: Claude Code does not support AGENTS.md — see tip below). Keep it minimal — your README should contain 90% of what AI needs; AGENTS.md adds only AI-specific operational context. That 10% is where the discipline lives — every token in AGENTS.md consumes primacy-zone attention that could go toward your actual task. Put project knowledge in your README where agents can read it on demand; AGENTS.md is for what changes _how_ the agent operates, not _what_ the project does. Reference external docs by link rather than inlining them — agents can fetch details when needed.

:::tip Claude Code: Using AGENTS.md with Claude Code
Claude Code uses `CLAUDE.md` instead of `AGENTS.md`. To maintain a single source of truth while supporting both ecosystems, use **@-linking** in your `CLAUDE.md`:

```markdown
# CLAUDE.md

@/AGENTS.md
```

This imports your AGENTS.md content into Claude's context. For details on Claude Code's hierarchical context system, see the [Claude Code documentation](/developer-tools/cli-coding-agents#claude-code).
:::

:::warning Hierarchical CLAUDE.md: Hidden Context Bloat
Unlike AGENTS.md (a single file with predictable token cost), Claude Code's hierarchical system merges `CLAUDE.md` files from multiple levels — project root, subdirectories, `.claude/rules/`, user config, and enterprise policy — all automatically loaded based on your working directory. Each additional file consumes primacy-zone tokens, compounding the attention pressure this lesson describes: every merged file pushes your actual task deeper into the forgotten middle. The total context-file token cost becomes invisible unless you audit every level of the hierarchy. If you use Claude Code, keep a single root `CLAUDE.md` that `@`-imports `AGENTS.md` and avoid scattering files throughout subdirectories — the hierarchy multiplies the squeeze.
:::

:::warning Security Consideration
Context files are injected directly into system prompts. Security researchers have identified "Rules File Backdoor" attacks where malicious instructions are injected using Unicode characters or evasion techniques. Keep context files minimal, version-controlled, and code-reviewed like any other code.
:::

## MCP Tools

Every tool the agent can call ships with a schema: name, description, parameters. Those schemas live in the request context and count toward input tokens. Built-in tools (Read, Edit, Bash) are modest at ~50–1000 tokens each. MCP servers are a different story entirely.

The real issue is placement, not just capacity. **Eager loading** puts tool definitions in the initial request context before the model plans its first action. **Deferred loading** keeps only a search tool and compact catalog metadata upfront, preserving attention for the user task until a capability is actually needed.

The cost is structural: every tool schema is serialized as JSON with repeated type annotations, nested `required` arrays, and verbose natural-language descriptions. A typical tool runs 300–600 tokens; complex API tools like GitHub's file operations hit 1,000–2,000. These costs compound quickly across multi-server setups. The governing rule is simple: **small, high-frequency toolsets favor eager loading; large, broad catalogs favor deferred loading.**

### Eager vs Deferred Loading

Eager loading keeps calls simple because the model already has the schema when it starts planning. Deferred loading protects the startup prompt by exposing ToolSearch first, but every deferred tool now depends on runtime discovery.

<ScrollDrivenFigure
  phaseEnd={0.5}
  caption="The MCP loading tradeoff: eager loading keeps schemas in the initial request so calls stay direct, but it pushes the task lower in context. Deferred loading keeps the startup prompt compact by exposing ToolSearch first, then discovers and loads schemas only when needed."
>
  <MCPToolSchemaDiagram />
</ScrollDrivenFigure>

**Takeaway:** the more your catalog behaves like infrastructure, the more you should optimize for prompt placement instead of immediate tool visibility. The same logic applies one layer up to reusable procedures: if a workflow library is broad, discover it cheaply and load the full instructions only when needed. The same automation-versus-reliability tradeoff applies here.

<details>
<summary>See the deferred-loading runtime sequence</summary>

When a deferred tool is needed, the model must **discover → load → call**:

<ContextStreamBlock
  entries={contextManagementStream}
  ariaLabel="ToolSearch discovers gh_pr_create on demand, loads its schema, then makes the tool callable"
/>

Deferred loading is powerful precisely because it turns schema exposure into a runtime workflow instead of paying the full cost at startup.
</details>

## Skills and Procedures — The Automation-vs-Reliability Tradeoff

Skills are the **procedure-side analogue of deferred MCP tools**. Deferred MCP exposes a cheap capability catalog up front, then loads a tool schema only when the tool is needed. Skill systems do the same for workflows: the agent sees a lightweight listing first, then loads the full procedure only when the task matches or you invoke it directly.

The critical difference from tool schemas is not the mechanism — it is who decides which procedure to activate. This is the **automation-vs-reliability tradeoff**: auto-discovery is friction-free but the agent may pick wrong, while manual invocation always works but requires the human to know the skill exists.

### Two Invocation Paths

<ScrollDrivenFigure
  phaseEnd={0.5}
  caption="Skill invocation paths: manual selection is reliable; model matching is automatic but probabilistic."
>
  <SkillsInvocationDiagram />
</ScrollDrivenFigure>

**Manual invocation**: The user explicitly selects a procedure — a slash command (`/commit`), skill picker, or sub-agent mention. The procedure loads because the human chose it. **Always reliable** — the model cannot misidentify the workflow — but the human must discover the skill and remember its name.

**Model invocation**: The agent sees the procedure catalog in context, matches your intent against the descriptions, and loads the relevant workflow automatically. **Zero friction** — the agent handles discovery — but the model may pick the wrong skill, miss the right one, or waste tokens evaluating irrelevant descriptions.

Most skill systems allow per-skill configuration of this tradeoff. Claude Code exposes `disable-model-invocation` (manual only) and `user-invokable` (model only) as frontmatter fields in each `SKILL.md`. A skill with `disable-model-invocation: true` never auto-triggers — it runs only when explicitly invoked. This lets you match the invocation path to the workflow's reliability requirements: critical workflows (deploy, commit, review) benefit from manual invocation; exploration workflows (search code, explain errors) work well with auto-discovery.

### Different Harnesses, Same Core Pattern

Unlike MCP — a defined protocol — skills are not standardized across tools. Each harness implements them differently. Yet they all follow the same pattern: advertise lightweight discovery metadata upfront, load the full procedure content only when invoked.

Claude Code lists skill names and descriptions, then loads `SKILL.md` when the skill is activated. Pi does the same and also exposes prompt templates as direct slash commands. Codex separates built-in slash commands from a `/skills` browser that inserts skill context only when selected. OpenCode leans more on reusable agents and `@`-invoked subagents, but the idea is identical: keep the startup prompt lean and bring in specialized instructions only when relevant.

That catalog listing is not free. Procedure metadata — names and descriptions used for discovery — consumes context budget even before any workflow runs. This is the procedure-side analogue of MCP schema pressure: different payload, same design problem.

The practical framing: **AGENTS.md** holds what the agent should always know — architecture, conventions, constraints. **Skills** hold what the agent should know how to do — specific workflows loaded on demand, with the invocation path controlling who decides which one runs.

## Sub-Agents — Context Isolation

Sub-agents run in completely separate context windows. This is the most powerful context engineering tool: a sub-agent might consume 50K–150K tokens researching a codebase, but returns only a 2K–5K synthesis to the orchestrator. **Zero raw research tokens in the parent context** — the search trail, file reads, and partial interpretations never existed as far as the main conversation is concerned.

Think of a sub-agent like a function call for agents. The prompt is the parameter: "Find all JWT authentication code and explain the current implementation." The synthesis is the return value: "JWT implementation found at src/auth/jwt.ts using Passport.js..." You pay for work in both contexts, but the parent receives the compact result instead of inheriting the whole research journey.

<ScrollDrivenFigure
  phaseEnd={0.55}
  caption="Sub-agent context isolation: research consumed internally, compact synthesis returned."
>
  <SubAgentDiagram />
</ScrollDrivenFigure>

The tradeoffs:

- **No shared state** — each sub-agent starts fresh, so you pay the context-file loading cost again
- **Synthesis can lose nuance** — the 2K summary omits details the parent might have needed
- **Cost multiplied** — every sub-agent is a separate API call with its own token consumption
- **No nesting** — sub-agents cannot spawn sub-agents; the orchestrator is always the root
- **10 parallel cap** — Claude Code limits concurrent sub-agents to 10

The trade-off is token cost, not accuracy. You pay to process tokens in both contexts, but the orchestrator stays clean. That often improves first-iteration accuracy enough to save tokens compared with multiple correction cycles from a polluted parent context.

Sub-agents are ideal for heavy research, multi-file analysis, and exploratory tasks where the journey matters less than the destination. They can also generate independent plans or implementations when you want to compare options before applying one result.

### Two Sub-Agent Architectures

**Autonomous architecture:** You give the agent tools and a system prompt. The agent autonomously decides what to do, which tools to call, and how to sequence its work.

Example: Claude Code's Explore agent. You send it a task, it autonomously picks tools and sequences, then delivers results. Dynamic and adaptable — it handles any task you throw at it without prior engineering.

**Structured architecture:** You build a deterministic control plane that defines the exact algorithm. The LLM makes tactical decisions within your structure ("Should I expand this symbol?" "Is this chunk relevant?"), but the overall flow is fixed.

Example: ChunkHound uses a fixed multi-hop pipeline where the system controls traversal strategy and the LLM ranks relevance at decision points. Deterministic and reproducible — same input, same results every time.

<ScrollDrivenFigure phaseEnd={0.6} caption="Two sub-agent architectures: deterministic pipeline (structured) vs. adaptive exploration (autonomous).">
  <ArchitectureDiagram />
</ScrollDrivenFigure>

**The architectural trade-off:** Autonomous agents are dynamic — they handle any task and adapt on the fly, but are non-deterministic (same prompt yields different paths each run) and bounded by context capacity. Structured agents are reproducible and scale to any information volume with better token efficiency, but only handle the specific task they were engineered for and cost more to build.

See [Lesson 5](/methodology/lesson-5-grounding#solution-2-sub-agents-for-context-isolation) for sub-agent grounding patterns.

### If You Cannot Use Sub-Agents

Without sub-agents, exploit the attention curve directly:

- Put durable constraints at the start, where primacy is strongest.
- Put the active task at the end, where recency is strongest.
- Keep supporting evidence in the middle summarized and referenced, not pasted wholesale.
- Start a fresh phase when the middle becomes crowded instead of continuing in a degraded thread.

This is less powerful than isolation, but it still respects the core rule: important facts must land where the model can attend to them.

## Context Compaction — Lossy Handoff {#runtime-management}

Compaction is lossy summarization. The model compresses the current session into a synthetic state guided by your instruction — but it cannot guarantee what survives. Future relevance is unknowable at compaction time: a debugging trace that looks like noise now may be the clue the next step needed.

The reliable alternative is **manual handoff with external checkpointing**. Before starting a new phase, write a brief state file with the current goal, changed files, decisions, unresolved errors, and next step. Files provide exact recall; a fresh context provides inference quality.

### The Buffer Tax

Compaction itself consumes working context. The system reserves a buffer to ensure it can still perform the handoff when the session nears capacity — capacity intentionally withheld from normal conversation. This is the compaction buffer: a tax on your effective window.

:::tip Disable auto-compaction
Auto-compaction trades usable context for a safety margin you don't need when you own handoffs explicitly. Both Claude Code (CLI settings) and [pi](https://pi.dev) let you disable it:

```json
{ "compaction": { "enabled": false } }
```

Reclaim the buffer for actual work. Compact only when you choose to — at phase boundaries — and always externalize anything that must survive exactly.
:::

---

## Key Takeaways

**Context files give guaranteed delivery at the cost of attention pressure**
Every token in AGENTS.md shifts the user task band deeper into the middle zone — keep files under 25KB.

**MCP tool schemas compound quickly across multi-server setups**
A single GitHub MCP server consumes ~55K tokens; three moderate servers can consume 72% of your window before the conversation starts. Enable ToolSearch for deferred loading when you have more than 20 tools.

**Skills and reusable procedures land in the recency zone**
Unlike context files that always consume tokens at startup, skill systems keep lightweight discovery metadata in context until invoked — and when they run, the expanded workflow lands in the recency zone where attention is strongest. The key design decision is the invocation path: manual invocation is always reliable but requires human awareness, while auto-discovery is friction-free but fallible. Per-skill configuration lets you choose the right path for each workflow.

**Sub-agents trade token cost for clean orchestrator context**
A sub-agent may consume 150K tokens internally but return only 2K to the parent — the research never touches the orchestrator's window. Use this for heavy research, multi-file analysis, exploratory tasks, and independent candidates that will be judged before one result is applied.

**Disable auto-compaction; own handoffs with checkpoint files**
Compaction is lossy summarization — it drops what looks irrelevant now but may be critical later. External checkpoint files provide reliable recall; a fresh context provides inference quality. If you must use compaction, externalize anything that must survive exactly.

**Choose the mechanism by what the knowledge is for**
Is it always needed? → Context file. Is it a callable tool? → MCP. Is it a workflow procedure? → Skill, prompt template, or reusable agent. Is it heavy research? → Sub-agent. Is context accumulating? → write a checkpoint file and start a fresh phase. Don't rely on auto-compaction.

---

**Next:** [Lesson 7: Reliability Levers](./lesson-7-reliability-levers.md)
